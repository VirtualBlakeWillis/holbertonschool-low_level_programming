#include "main.h"
/**
  * _abs - return absolute value of int
  *@x: input value
  * Return: input value as an absolute value
  */
int _abs(int x)
{
	if (x > 0)
		return (x);
	else
		return (x * -1);
}
